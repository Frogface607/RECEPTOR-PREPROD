# lib/mock

Deterministic sandbox fixtures used when real iiko credentials are absent.

These fixtures keep local development, preview deployments, and tests stable.
Production venues should use saved iiko credentials from onboarding.
